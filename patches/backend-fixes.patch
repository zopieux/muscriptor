diff --git a/muscriptor/utils/audio.py b/muscriptor/utils/audio.py
index 18037eb..01c8146 100644
--- a/muscriptor/utils/audio.py
+++ b/muscriptor/utils/audio.py
@@ -70,12 +70,48 @@ def _read_non_wav_file(source: str | Path | IO[bytes]) -> tuple[torch.Tensor, in
             "Install with: `pip install soundfile` or `uvx --with soundfile`"
         ) from e
 
-    target = str(source) if isinstance(source, (str, Path)) else source
-    data, sample_rate = sf.read(target, dtype="float32")
-    if data.ndim == 1:
-        data = data[:, None]
-    wav = torch.from_numpy(np.ascontiguousarray(data.T))
-    return wav, sample_rate
+    try:
+        target = str(source) if isinstance(source, (str, Path)) else source
+        data, sample_rate = sf.read(target, dtype="float32")
+        if data.ndim == 1:
+            data = data[:, None]
+        wav = torch.from_numpy(np.ascontiguousarray(data.T))
+        return wav, sample_rate
+    except Exception:
+        try:
+            if isinstance(source, (str, Path)):
+                import torchaudio
+                wav, sample_rate = torchaudio.load(str(source))
+                return wav.float(), sample_rate
+            raise ValueError("torchaudio requires filepath")
+        except Exception:
+            import io, os, subprocess, tempfile
+            tmp_in = None
+            if isinstance(source, (str, Path)):
+                in_path = str(source)
+            else:
+                if hasattr(source, "seek"):
+                    source.seek(0)
+                data_in = source.read() if hasattr(source, "read") else bytes(source)
+                tmp_in = tempfile.NamedTemporaryFile(suffix=".m4a", delete=False)
+                tmp_in.write(data_in)
+                tmp_in.flush()
+                tmp_in.close()
+                in_path = tmp_in.name
+
+            try:
+                cmd = ["ffmpeg", "-y", "-i", in_path, "-vn", "-acodec", "pcm_s16le", "-f", "wav", "pipe:1"]
+                proc = subprocess.run(cmd, check=True, stdout=subprocess.PIPE, stderr=subprocess.DEVNULL)
+                wav, sr = _read_wav_file(io.BytesIO(proc.stdout))
+                if wav.shape[-1] == 0:
+                    raise ValueError("Decoded audio contains 0 samples")
+                return wav, sr
+            finally:
+                if tmp_in is not None and os.path.exists(tmp_in.name):
+                    try:
+                        os.unlink(tmp_in.name)
+                    except OSError:
+                        pass
 
 
 def resample(
diff --git a/muscriptor/utils/download.py b/muscriptor/utils/download.py
index 1b1150d..cf57013 100644
--- a/muscriptor/utils/download.py
+++ b/muscriptor/utils/download.py
@@ -4,13 +4,20 @@ import hashlib
 import os
 import urllib.request
 from pathlib import Path
-from huggingface_hub import hf_hub_download
-from huggingface_hub.errors import (
-    GatedRepoError,
-    HfHubHTTPError,
-    RepositoryNotFoundError,
-)
-from huggingface_hub.utils import EntryNotFoundError
+try:
+    from huggingface_hub import hf_hub_download
+    from huggingface_hub.errors import (
+        GatedRepoError,
+        HfHubHTTPError,
+        RepositoryNotFoundError,
+    )
+    from huggingface_hub.utils import EntryNotFoundError
+except ImportError:
+    hf_hub_download = None
+    class GatedRepoError(Exception): pass
+    class HfHubHTTPError(Exception): pass
+    class RepositoryNotFoundError(Exception): pass
+    class EntryNotFoundError(Exception): pass
 
 
 _CACHE_DIR = Path.home() / ".cache" / "muscriptor"
diff --git a/pyproject.toml b/pyproject.toml
index a52fa6f..665800d 100644
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -47,13 +47,12 @@ dependencies = [
     "mido>=1.3",
     "packaging>=20.0",
     "safetensors>=0.4",
-    "huggingface_hub>=0.13",
     "typer>=0.10",
     "fastapi>=0.136.3",
     "uvicorn[standard]>=0.48.0",
     "httpx>=0.28.1",
     "python-multipart>=0.0.29",
-    "soundfile>=0.14.0",
+    "soundfile",
     "beat-this>=1.1",  # Tempo and downbeat tracking
 ]
 
